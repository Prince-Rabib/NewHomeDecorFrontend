import classes from './productSummary.module.css';

const ProductsSummary = () => {
  return (
    <section className={classes.summary}>
      <h2>You name it and have it</h2>
      <p>
      Home Decord BD is a dedicated online retailing store that thrives to provide it's customer with the best available roduct at the lowest possible price.
      </p>
    </section>
  );
};

export default ProductsSummary;
